<div class="sidebar">
  <div class="box auth">
    <% if(!user.id && !user.login) { %>

    <form method="post" class="login">
      <h2>Вход</h2>
      <div class="form-group">
        <label for="login-login">Логин</label>
        <input type="text" id="login-login" name="login" />
      </div>
      <div class="form-group">
        <label for="login-password">Пароль</label>
        <input type="password" id="login-password" name="password" />
      </div>
      <div class="buttons">
        <button class="login-button">Войти</button>
        <button class="switch-button secondary-inverse">Регистрация</button>
      </div>
    </form>

    <form method="post" class="register">
      <h2>Регистрация</h2>
      <div class="form-group">
        <label for="register-login">Логин</label>
        <input type="text" id="register-login" name="login" />
      </div>
      <div class="form-group">
        <label for="register-password">Пароль</label>
        <input type="password" id="register-password" name="password" />
      </div>
      <div class="form-group">
        <label for="register-password-confirm">Повторить пароль</label>
        <input
          type="password"
          id="register-password-confirm"
          name="passwordConfirm"
        />
      </div>
      <div class="buttons">
        <button class="register-button">Регистрация</button>
        <button class="switch-button secondary-inverse">Вход</button>
      </div>
    </form>

    <% } else { %>
    <ul>
      <li>
        Hello,
        <a href="/users/<%= user.login %>"> <%= user.login %></a>!
      </li>
      <li><a href="/post/add">Add job post</a></li>
      <li><a href="/api/auth/logout">Exit</a></li>
    </ul>
    <% } %>
  </div>
  <div class="box">
    <div>
      Choose your language:
      <ul>
        <li><a href="/?clang=ua">Ukrainian</a></li>
        <li><a href="/?clang=en">English</a></li>
      </ul>

      <p><%= translation.WELCOME_MSG %></p>
    </div>
  </div>
</div>
